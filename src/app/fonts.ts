import { Inter } from "next/font/google";
import { Sigmar_One } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const sigmarOne = Sigmar_One({ weight: "400", preload: false });
